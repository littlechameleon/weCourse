<!--index.wxml-->
<view class="container">
    <!-- 用户登录测试 -->
    <view class="userinfo">
        <image
            class="userinfo-avatar"
            src="{{logged ? userInfo.avatarUrl : './user-unlogin.png'}}"
            background-size="cover"
        ></image>
        <view>
            <button class="userinfo-nickname" wx:if="{{!logged}}" open-type="getUserInfo" bindgetuserinfo="bindGetUserInfo">点击登录</button>
            <text class="userinfo-nickname" wx:else>{{userInfo.nickName}}</text>
        </view>
    </view>


    <!-- 上传图片测试 -->
    <!-- <view class="uploader">
        <view class="uploader-text" bindtap="doUpload">
            <text>上传图片</text>
        </view>
        <view class="uploader-container" wx:if="{{imgUrl}}">
            <image
                class="uploader-image"
                src="{{imgUrl}}"
                mode="aspectFit"
                bindtap="previewImg"
            ></image>
        </view>
    </view> -->


    <!-- 上传图片测试 -->
    <!-- <view class="uploader">
        <navigator url="../addCgi/addCgi" open-type="navigate" class="uploader-text">
            <text>快速增加 CGI</text>
        </navigator>
    </view> -->

    <view class='teaching page__teaching'>
      <view class='learning-course-title'>
        <text>学习 </text>
        <switch bindchange='switchChange'/>
        <text> 教授</text>
      </view>
      <view class='course-list'>
        <view wx:for="{{courseList}}" data-id='{{item.course_id}}' wx:key='course_id' class='course'  bindtap="enterCourse"> 
          <text class='course-title'>{{item.title}}</text>
        </view>

        <view bindtap="createCourse" class="course" wx:if='{{type !== 0}}'>
          <image class='add-image' src="../../assets/image/add.png" mode="cover"></image>
        </view>
      </view>
    </view>

        <!-- 上传图片测试 -->
    <view class="uploader">
        <navigator url="../addCourse/addCourse" open-type="navigate" class="uploader-text">
            <text>待定</text>
        </navigator>
    </view>



</view>
